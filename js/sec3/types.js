/* eslint-disable*/
// Types

// String
/*
var name = "moumen";

var nameTwo = 'moumen';

var nameThree = `moumen`;


var names = "moumen \"soliman\""

var helloWorld = "hello"+ " " + nameTwo + " " + "welocme";

var helloWorldBackticks = `hello 

welcome

${nameTwo} ${100 -1}`

var html = `
    <div>
        <h2>${nameThree}</h2>
        <p>${helloWorld}</p>
    </div>
`

document.body.innerHTML = html;
*/

// Numbers

/* var age = 25.5;

var ageTwo = 25;

var eachAges = Math.round(Math.random()) */

// Objects

/* const car = {
    name: "volvo",
    color: "red",
    model: "2007",
    newObj: {
        name: "toyota"
    }
}

car.newName = "volovo 2"; */

// null & undefined
/* var car;

var carNew = null;

setTimeout(() => carNew = "string",5000) */

// Boolean & Equality & Symbol
/*
var isColorRed = false;

var isColorBlue = true;

var noSymbole = "foo";
var noSymbole1 = "foo";

var sumboly1 = Symbol("foo");
var sumboly2 = Symbol("foo");
*/